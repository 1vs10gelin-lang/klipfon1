# Production must use its exact HTTPS origin, without trailing slash.
APP_ORIGIN=http://localhost:3000
DATA_DIR=.data
# Generate a fresh secret, at least 32 random characters; never commit it.
KLIPFON_SETUP_SECRET=
