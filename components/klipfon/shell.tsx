'use client';
import {Sidebar,SidebarProvider,SidebarHeader,SidebarContent,SidebarFooter,SidebarMenu,SidebarMenuItem,SidebarMenuButton,SidebarTrigger,SidebarInset,SidebarGroup} from '@/components/ui/sidebar';
import {Toaster} from '@/components/ui/sonner';
import {Brand,labels} from './shared';
import {LogOut,ArrowUpRight} from 'lucide-react';
export function Shell({user,section,setSection,items,children,admin=false}:{user:any,section:string,setSection:(v:string)=>void,items:{id:string,label:string,icon:any}[],children:React.ReactNode,admin?:boolean}){return <SidebarProvider><Sidebar collapsible="offcanvas" className="border-r border-border"><SidebarHeader className="px-6 py-7"><Brand/><span className="text-xs tracking-widest text-muted-foreground mt-3">{admin?'YÖNETİM MERKEZİ':'ÜRETİCİ PLATFORMU'}</span></SidebarHeader><SidebarContent><SidebarGroup className="px-4"><SidebarMenu>{items.map(({id,label,icon:Icon})=><SidebarMenuItem key={id}><SidebarMenuButton isActive={id===section} className="min-h-12 text-sm px-4 gap-3 rounded-xl" onClick={()=>{setSection(id);window.history.replaceState({},'',(admin?'/admin':'/panel')+'?bolum='+id)}}><Icon size={18}/><span>{label}</span></SidebarMenuButton></SidebarMenuItem>)}</SidebarMenu></SidebarGroup><div className="mx-5 mt-auto mb-5 p-4 border border-border rounded-xl"><div className="text-sm font-semibold mb-2">Bir sorunda yalnız değilsin.</div><a href="/yardim" className="subtle flex gap-2 items-center">Yardım merkezine git <ArrowUpRight size={14}/></a></div></SidebarContent><SidebarFooter className="p-5 border-t border-border"><div className="flex items-center gap-3 mb-3"><div className="creator-initial shrink-0">{user?.name?.slice(0,1)||'K'}</div><div className="min-w-0"><div className="text-sm font-semibold truncate">{user?.name||'Hesabım'}</div><div className="text-xs text-muted-foreground">{labels[user?.role]||'Klipfon'}</div></div></div><form action="/api/auth/logout" method="post"><button className="text-sm text-muted-foreground flex gap-2 items-center" type="submit"><LogOut size={15}/> Çıkış yap</button></form></SidebarFooter></Sidebar><SidebarInset className="bg-background min-w-0"><div className="mobile-only px-4 py-3 border-b border-border"><SidebarTrigger aria-label="Menüyü aç"/></div><main className="dashboard-main">{children}</main></SidebarInset><Toaster position="top-right"/></SidebarProvider>}
